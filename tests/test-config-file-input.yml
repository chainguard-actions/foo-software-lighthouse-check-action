name: Test configFile input
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
      - name: Create lighthouse config file
        run: |
          cat > /tmp/lighthouse-config.json << 'EOF'
          {
            "urls": ["https://www.example.com"],
            "verbose": true
          }
          EOF
      - id: action
        uses: .
        continue-on-error: true
        with:
          configFile: '/tmp/lighthouse-config.json'
          verbose: 'true'
      - name: Assert action ran (configFile input accepted)
        env:
          OUTCOME: ${{ steps.action.outcome }}
        run: |
          echo "outcome: $OUTCOME"
          if [ "$OUTCOME" = "success" ] || [ "$OUTCOME" = "failure" ]; then
            echo "PASS: action ran with configFile input (outcome: $OUTCOME)"
          else
            echo "FAIL: unexpected outcome '$OUTCOME'"
            exit 1
          fi
